# Currency Symbol
NEXT_PUBLIC_CURRENCY_SYMBOL = '$'

# Admin Secret Key — Protège toutes les routes d'écriture (POST, PUT, DELETE)
# Changer cette valeur en production par une chaîne aléatoire longue et sécurisée.
# Générer avec : openssl rand -hex 32
ADMIN_SECRET_KEY = 'your-very-long-random-secret-here'
